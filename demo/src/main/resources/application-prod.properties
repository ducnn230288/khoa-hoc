# --- prod profile ---

# Session
server.servlet.session.timeout=30m
server.servlet.session.cookie.secure=true

# Springdoc: DISABLED in prod (SEC-8, AC-21)
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# CORS: TODO set prod origin (OI-A')
# allowed.origins=https://your-prod-frontend.example.com

# NOTE: Do NOT put real credentials here. Use environment variables:
#   DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD (SEC-10)
